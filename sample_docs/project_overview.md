# Chat With Your Personal Docs

This project is a minimal CLI that answers questions from `.txt`, `.md`, and `.pdf` files in a folder.

It chunks documents, embeds them with OpenAI, retrieves the top matches by cosine similarity, and answers using only those excerpts.
